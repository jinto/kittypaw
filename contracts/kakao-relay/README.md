# Kakao Relay Contract

Produced by `services/kakao`.

Consumed by `apps/kittypaw`.

This contract covers only the relay WebSocket frames between the Kakao gateway
and local Kittypaw. Kakao OpenBuilder's upstream webhook payload is vendor-owned
and should be documented separately if needed.
