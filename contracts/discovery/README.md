# Discovery Contract

Produced by `apps/kittyapi`.

Consumed by `apps/kittypaw`.

The discovery response tells local clients where the current first-party
services live. Empty optional URLs mean the corresponding feature is unavailable
or not configured.
