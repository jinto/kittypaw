# apps/kittypaw

Local KittyPaw product:

- CLI
- local daemon
- local web UI
- engine and skill runtime
- local account store
- channel adapters
- hosted chat outbound connector

This directory will eventually contain the current `kittypaw` repository.

Release tags use the `kittypaw/v*` prefix.
