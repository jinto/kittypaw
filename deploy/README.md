# Deploy

Deployment assets are grouped by deployable unit.

```text
deploy/
  kittyapi/
  chat/
  kakao/
```

`apps/kittypaw` release automation lives with the app because it produces GitHub
release assets rather than a hosted service deployment.
