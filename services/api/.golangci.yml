version: "2"

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/kittypaw-app/kittyapi

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - misspell

  settings:
    revive:
      rules:
        - name: exported
          disabled: true
    misspell:
      locale: US

run:
  timeout: 3m

issues: {}
